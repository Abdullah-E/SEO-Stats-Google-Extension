-LOGIN
--website X
    -- show user profile, after logged in.  
    -- logout button

--backend
    --user profile

--extension google login X
    --popup login pyaara

-PAYMENT OPTIONS - reliant on backend

miscellaneous
    --react-google-login deprecated

TIMELINE:
    --merge repos ♥ (abd)
    
    --login minor additions/changes in website (abd) ☻
    --dashboard big work heavy lifting (sala) ☻
    --login minor additions/changes in extension (abd) 
    --login and user profile backend (sala) ☻

    --PAYMENT
    --payment api etc (probably stripe/paddle) (abd)
    
    --CREDITS system
    --backend functions (sala)
       --add credits (googleID)
       --get credits
    --credits displaying on frontend (abd)

    --buy credits button(s)

    --deduct credits on query from extension 
    BACKEND SETUP:
        --Setup server (Express.js)
        --Database setup (MongoDB)
        --Create models (User, Payment, etc.)
        --Setup routes
            --Auth routes (Login, Register, Logout)
            --User routes (Get profile, Update profile)
            --Payment routes (Initiate payment, Verify payment)
        --Setup controllers for each route
        --Implement middleware for authentication and error handling
        --Setup environment variables
        --Test endpoints with Postman
